<!DOCTYPE html><html lang="hu"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $emailTemplate->name }} statisztika | KwizzGo</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100 text-slate-900">@include('layouts.navigation')
<main class="mx-auto max-w-6xl px-4 py-8 sm:px-6">
<a href="{{ route('admin.email-templates.index') }}" class="font-black text-indigo-700">← E-mail sablonok</a>
<header class="mt-5 rounded-3xl bg-slate-950 p-7 text-white"><p class="text-xs font-black uppercase tracking-widest text-amber-300">Kiküldési statisztika</p><h1 class="mt-2 text-3xl font-black">{{ $emailTemplate->name }}</h1><p class="mt-2 text-slate-300">{{ $emailTemplate->subject }}</p><div class="mt-5 flex gap-3"><span class="rounded-full bg-white/10 px-4 py-2 font-black">{{ $stats['sent'] }} kiküldve</span><span class="rounded-full bg-emerald-400/20 px-4 py-2 font-black text-emerald-200">{{ $stats['opened'] }} megnyitva</span></div></header>
<div class="mt-5 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">A megnyitás követőképpel mérhető. Ha a levelezőprogram blokkolja a képeket, vagy adatvédelmi proxy tölti le őket, a mérés eltérhet a tényleges megnyitástól.</div>
<section class="mt-6 overflow-hidden rounded-3xl bg-white shadow-xl"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">Felhasználó</th><th class="px-5 py-4">Kiküldve</th><th class="px-5 py-4">Megnyitva</th></tr></thead><tbody class="divide-y">@forelse($deliveries as $delivery)<tr><td class="px-5 py-4"><strong>{{ $delivery->user?->username ?: $delivery->user?->name }}</strong><div class="text-slate-500">{{ $delivery->user?->email }}</div>@if($delivery->user)<a href="{{ route('admin.users.show',$delivery->user) }}" class="text-xs font-black text-indigo-700">User adatlap →</a>@endif</td><td class="px-5 py-4">{{ \App\Support\LocalDateTime::format($delivery->sent_at) }}</td><td class="px-5 py-4">@if($delivery->opened_at)<span class="font-black text-emerald-700">{{ \App\Support\LocalDateTime::format($delivery->opened_at) }}</span>@else<span class="text-slate-400">Nem mérhető / még nem nyitotta meg</span>@endif</td></tr>@empty<tr><td colspan="3" class="p-10 text-center text-slate-500">Ebből a levélből még nem ment ki példány.</td></tr>@endforelse</tbody></table></div>@if($deliveries->hasPages())<div class="border-t p-5">{{ $deliveries->links() }}</div>@endif</section>
</main></body></html>
